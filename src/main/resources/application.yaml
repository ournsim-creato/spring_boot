spring:
  application:
    name: spring-boot-api-p-2

  datasource:
    url: jdbc:postgresql://localhost:5432/Course1
    username: postgres
    password: "Simkonbong11@&$"

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 2s

  mail:
        host: smtp.gmail.com
        port: 587
        username: ournsim@gmail.com
        password: seua jfnr lktr llej
        properties:
          mail:
            smtp:
              auth: true
              starttls:
                enable: true  # required for port 587 (TLS upgrade)
              connectiontimeout: 5000
              timeout: 3000
              writetimeout: 5000

jwt:
  secret: ${JWT_SECRET:change-me-to-a-long-random-secret-at-least-32-bytes-please}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

# AES-256 key for encrypting captcha codes in Redis
# Generate a new one: openssl rand -base64 32
encryption:
  secret-key: ${ENCRYPTION_SECRET_KEY:GoYeAriehFXooFM12206sP02E646czNwD8wi3mB4RL8=}


captcha:
  enabled: ${CAPTCHA_ENABLED:true}           # false → generate returns enabled=false
  length: ${CAPTCHA_LENGTH:4}                # characters in the image code
  ttl-minutes: ${CAPTCHA_TTL_MINUTES:3}      # Redis key lifetime
  master-code: ${CAPTCHA_MASTER_CODE:}       # optional dev bypass (leave empty in prod)
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:urp-redis}
  max-size-mb: ${MINIO_MAX_SIZE_MB:1}
  create-bucket-if-missing: ${MINIO_CREATE_BUCKET:true}

  otp:
    secret-key: ${OTP_SECRET_KEY:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=}
    ttl-minutes: ${OTP_TTL_MINUTES:10}           # code validity window
    max-send-count: ${OTP_MAX_SEND_COUNT:5}      # lockout after N sends
    cooldown-minutes: ${OTP_COOLDOWN_MINUTES:5}  # min gap between resends